@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="_isDarkMode" Theme="Configuration.Theme"/> @* vai criar uma paleta de cores com o que foi personalizado (deixar sem nada para o padrão) *@
@* <MudDialogProvider />  *@
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        Dima
        <MudSpacer />
        <MudSwitch @bind-Value="_isDarkMode" />
    </MudAppBar>
    <MudMainContent>
        <MudContainer>
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool _isDarkMode { get; set; } = true;
}